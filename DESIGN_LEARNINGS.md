# KISSA MIKAZUKI Design Learnings

## 目的

showa-retroペルソナ（昭和を直接知らない30代男性）は、過剰な「セピアフィルター + RETRO英字」の手垢ついた表現に冷めた目を持っている。本サイトは、「実在の純喫茶を改装した古道具部」という設定で、印刷ノスタル感の中に楽しい組版を組み立てる方向に振った。

このブランドは架空店舗であり、実在の店舗・商品ではない。

## 設計したこと

- ロゴ ・ 看板を Reggae One、見出しを Hina Mincho、本文を Klee One、モノを DotGothic16、装飾を Yuji Syuku、と日本語書体だけで5書体を組み合わせた。
- ヒーローに回転する LP レコードとクリームソーダSVGを配置し、純喫茶のテーブル上をそのまま再現した。
- レコード棚はジャケットからレコード盤がスライドして出てくる hover を実装し、「触れる」喜びを残した。
- 「マッチ箱コレクション」セクションで、実在しない8つの昭和喫茶のマッチ箱を並べ、世界観の厚みを作った。

## CSS

主要なCSS判断:

```css
:root {
  --cha:#3a2418;          /* 深焙煎ブラウン */
  --butter:#f4e4c1;        /* バタークリーム */
  --aka:#c93b3b;           /* レトロ赤 */
  --midori:#3a8f7e;        /* クリームソーダ */
  --font-kanban:'Reggae One','Hina Mincho',serif;
  --font-dot:'DotGothic16',monospace;
}

/* 看板の3D影 */
.hero h1 {
  text-shadow:
    3px 3px 0 var(--butter-asa),
    6px 6px 0 var(--cha-koi);
}

/* レコード回転 */
@keyframes spin { to { transform: rotate(360deg) } }
.record { animation: spin 12s linear infinite }

/* レコード棚の hover で盤がスライド */
.lp:hover::after { right: -50% }
```

## アニメーション

- レコード回転は 12s 等速、`prefers-reduced-motion` で停止。
- ふわっとした `pop` リビールを各セクションに割り当て、ジュークボックスから音が立ち上がるような印象を作った。
- 「OPEN 一一時」表示の緑色サインを 1.6s で点滅させ、ネオン感を控えめに付与。

## フォント

- 看板: `Reggae One`（太い和製手書き）
- 見出し: `Hina Mincho`（雛明朝、装飾的）
- 本文: `Klee One`（教科書体）
- モノ: `DotGothic16`（8-bitっぽい和フォント）
- アクセント: `Yuji Syuku`（重厚な明朝）

英字（Bebas Neue 等）は不採用。「あの時代の喫茶店」は日本語で書かれていた、という事実に立ち返った。

## ボタン

ボタンはレトロ広告のパーツとして作る。

- 「お席を予約する」CTA は朱赤＋バタークリーム枠線＋ボックスシャドウで、看板広告のような押せ感を出した。
- 「予約する」のような商業文ではなく、「席を取る」というメニュー的書字を採用。

## UI/UX

- ヘッダー右に「OPEN 一一時」のドット文字 + 緑LED風サインを置き、喫茶店の入口感を再現。
- 商品カード（メニュー）に「No. 001」「マッチ箱」のような書き付けを配し、メニューブックの体裁を維持。
- 店主の手記は紙のレターパッド風（白い紙に -.4deg の回転、赤いシャドウ）。

## レイアウト

セクション間に「おしながき帯」というダーク反転 CTA 帯を1本だけ挿入し、商品列伝とレコード棚の間にリズムを作った。

レコード棚は 6 LPの 3×2 グリッド。各ジャケットには 6色のグラデーション（深緑→焦げ茶、橙→深紅、etc）を割り当て、「色から思い出される時代」を直感させた。
